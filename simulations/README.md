# simulations

**Run with flag**
``` 
    stack run simulation --profile -- +RTS -p

    -p - profile report
    -h - heap usage report (see util hp2ps)
```

**Setting cost centes**
```
    {-# SCC "answer yes" #-}
```